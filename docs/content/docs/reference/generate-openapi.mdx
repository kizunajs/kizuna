---
title: generateOpenApi
description: Generate an OpenAPI 3.1.0 document from a ts-kizuna api.
---

Generate an [OpenAPI 3.1.0](https://spec.openapis.org/oas/v3.1.0) document from your api. Returns a renderer you call with `'json'` or `'yaml'`.

<InstallTabs packageName="@ts-kizuna/openapi" />

```ts
import { generateOpenApi } from '@ts-kizuna/openapi';
```

## Parameters

```ts
generateOpenApi(api: Contract, overrides?: Partial<GenerateOpenApiOptions>): OpenApiRenderer
```

| Parameter   | Type                              | Description                                                    |
| ----------- | --------------------------------- | -------------------------------------------------------------- |
| `api`       | `Contract`                        | The `api` from [`defineConfig`](/docs/reference/define-config) |
| `overrides` | `Partial<GenerateOpenApiOptions>` | Merged over the plugin's options, for what only a build knows  |

It lives on `@ts-kizuna/openapi`, beside [`openApiPlugin`](/docs/reference/openapi-plugin) and the document types.

## Options

Options are declared on [`openApiPlugin`](/docs/reference/openapi-plugin), and `generateOpenApi` reads them off the api, so a build step cannot write a document that differs from the one your server serves. Pass `overrides` for what only a build step knows, such as the public `servers` list.

| Option            | Type                                  | Default   | Description                                                                             |
| ----------------- | ------------------------------------- | --------- | --------------------------------------------------------------------------------------- |
| `info`            | `{ title, version, description? }`    | required  | OpenAPI `info` object                                                                   |
| `openApiVersion`  | `'3.1.0'`                             | `'3.1.0'` | OpenAPI specification version for the generated document                                |
| `servers`         | `{ url, description? }[]`             | none      | Server URLs                                                                             |
| `setOperationId`  | `boolean \| 'concatenated-path'`      | `false`   | Set `operationId` from the route key. Use `'concatenated-path'` to include parent keys. |
| `externalDocs`    | `{ url, description? }`               | none      | Top-level external documentation link                                                   |
| `operationMapper` | `(operation, route, id) => operation` | none      | Callback to transform each operation before it is added to the spec                     |
| `derivedHead`     | `boolean`                             | `false`   | Document the derived `head` operation on every `GET` path without a declared one        |

There are no security or tag options. Security is emitted from [each route's `auth`](#security-emission) and the document's tag list from [`k.tags`](/docs/reference/k-tags), so neither can disagree with what the server enforces.

## Returns

An `OpenApiRenderer` function:

```ts
renderer('json'); // OpenApiDocument object
renderer('yaml'); // YAML string
```

## Example

```ts title="kizuna.config.ts"
import { defineConfig } from '@ts-kizuna/core';
import { openApiPlugin } from '@ts-kizuna/openapi';
import { routes } from './src/routes';

export default defineConfig({
    adapter: expressAdapter(),
    routes,
    plugins: [
        openApiPlugin({
            info: {
                title: 'My API',
                version: '1.0.0',
            },
            servers: [
                {
                    url: 'https://api.example.com',
                    description: 'Production',
                },
            ],
            setOperationId: true,
        }),
    ],
});
```

```ts title="scripts/generate-openapi.ts"
import { generateOpenApi } from '@ts-kizuna/openapi';
import kizuna from '../kizuna.config';

const spec = generateOpenApi(kizuna.api);

app.get('/openapi.json', (_req, res) => {
    res.json(spec('json'));
});
```

## Security emission

Security is generated from what you declared, not configured on the generator. Every [identity](/docs/reference/k-identity) named under `auth.identities` on [`defineConfig`](/docs/reference/define-config) is emitted under `components.securitySchemes`, and every route with an [`auth`](/docs/reference/k-route) references those names in its `security`, with the permissions an OAuth route requires as its scopes. Public routes (`false`) get none.

With the `user` (bearer) and `member` (API key) identities, and a workspace route declaring `auth: 'member'`, the document contains:

```yaml title="openapi.yaml"
components:
    securitySchemes:
        user:
            type: http
            scheme: bearer
        member:
            type: apiKey
            name: x-workspace-token
            in: header
paths:
    /workspace:
        get:
            security:
                - member: []
```

A [`custom`](/docs/reference/k-identity#custom-identities) identity is the exception: OpenAPI can't describe its credential, so it emits no `securityScheme` and no `security`. Its routes instead carry an `x-kizuna-guarded` extension, keeping a protected-out-of-band route distinct from a public one.

A route with `roles` carries them under `x-kizuna-roles`, `['owner']`, and one with `requires` carries the permissions under `x-kizuna-requires`, `{ workspace: ['delete'] }`, so a reader of the document sees who the caller has to be.

The spec and the runtime [guards](/docs/reference/k-identity) both derive from the same `auth`, so documented security is enforced security.

See the [OpenAPI guide](/docs/openapi) for Scalar integration, writing specs to disk, and breaking change detection.
