{
  "name": "@kizunajs-demo/swift",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate": "pnpm --filter @kizunajs-demo/express kizuna:generate",
    "pretest": "pnpm generate",
    "test": "tsx scripts/test.ts",
    "build": "cd swift && swift build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kizunajs-demo/shared": "workspace:*",
    "@kizunajs/swift": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
